{
  "name": "bashmak",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:arch": "node scripts/check-architecture.mjs",
    "sim:balance": "tsx scripts/balance_sim.ts",
    "build": "node scripts/check-architecture.mjs && tsc && vite build && esbuild server/src/index.ts --bundle --format=esm --target=es2022 --platform=neutral --outfile=dist/_worker.js",
    "preview": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name bashmak"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.53.1",
    "playwright": "^1.62.1",
    "tsx": "^4.23.13",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "wrangler": "^4.125.0"
  },
  "dependencies": {
    "hono": "^4.13.5",
    "phaser": "^4.2.1"
  }
}
